fun fib(n) {
  if (n < 2) return n;
    return fib(n - 1) + fib(n - 2);
}

var before = clock();
// calculate the 20th Fibonacci number
/*
 * Block comment /* nested block comment */
*/
print fib(20);
var after = clock();
print after - before;
